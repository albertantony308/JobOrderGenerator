<Window x:Class="ClientApp.ActivationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Service Memo Manager - Activation" Height="450" Width="600"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">
    <Grid>
        <Border Background="{StaticResource SurfaceContainerLowestBrush}" 
                CornerRadius="6" 
                Margin="40" 
                Padding="30"
                VerticalAlignment="Center">
            <!-- Ambient shadow simulation -->
            <Border.Effect>
                <DropShadowEffect Color="#191c1e" Opacity="0.06" BlurRadius="40" Direction="270" ShadowDepth="12"/>
            </Border.Effect>

            <StackPanel>
                <TextBlock Text="Welcome" FontSize="24" FontWeight="Bold" Margin="0,0,0,10" Foreground="{DynamicResource OnSurfaceBrush}"/>
                <TextBlock Text="Please enter your Activation Key to continue. An internet connection is required for first-time activation." 
                           TextWrapping="Wrap" Foreground="{DynamicResource OnSurfaceVariantBrush}" Margin="0,0,0,20"/>
                
                <!-- Input Field -->
                <Border Background="{StaticResource SurfaceContainerHighestBrush}" CornerRadius="4" Padding="10" Margin="0,0,0,20">
                    <TextBox x:Name="KeyInput" Background="Transparent" BorderThickness="0" FontSize="16" Foreground="{StaticResource OnSurfaceBrush}"/>
                </Border>
                
                <TextBlock x:Name="ErrorText" Foreground="IndianRed" Margin="0,0,0,10" Visibility="Collapsed"/>

                <!-- Primary CTA -->
                <Button Content="Activate" x:Name="ActivateButton" Click="ActivateButton_Click" 
                        Foreground="{StaticResource OnPrimaryBrush}"
                        Padding="0,12" FontSize="14" FontWeight="SemiBold"
                        BorderThickness="0" Margin="0,0,0,15">
                    <Button.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#004f96" Offset="0"/>
                            <GradientStop Color="#0067c0" Offset="1"/>
                        </LinearGradientBrush>
                    </Button.Background>
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <!-- Free Trial Link -->
                <TextBlock TextAlignment="Center" FontSize="13">
                    <Hyperlink NavigateUri="https://servicememomanager.com/freetrial" RequestNavigate="Hyperlink_RequestNavigate" Foreground="{DynamicResource PrimaryBrush}">
                        Don't have a subscription key? Get a free trial
                    </Hyperlink>
                </TextBlock>
            </StackPanel>
        </Border>
    </Grid>
</Window>
